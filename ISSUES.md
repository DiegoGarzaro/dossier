# Dossier — Issue & Tech-Debt Ledger

> Master ledger of every **gap, bug, or improvement** discovered while building Dossier.
> This is the source of truth for defects and debt; forward-looking scope/tasks live in [`PLAN.md`](PLAN.md).

**Working agreement:** the moment something is discovered — even if unrelated to the current task,
even if fixed immediately — it gets a row here. Never fix silently or rely on memory.

- **Type:** `Bug` (broken behaviour) · `Gap` (missing but planned) · `Improve` (works, could be better)
- **Severity:** 🔴 high · 🟠 med · 🟡 low
- **Status:** `open` · `in progress` · `done (YYYY-MM-DD)`
- **Ids** are stable: append as `G-NN`, never renumber or reuse. Close an item by setting its status to `done` (keep the row for history).

**Last updated:** 2026-08-13 (rev 8)

---

## Open & in progress

| ID | Type | Sev | Area | Finding & intended action | Status |
|---|---|---|---|---|---|
| G-61 | Bug | 🟠 | CI/release | **Broke the v0.2.0 release pipeline:** the new G-58 regression test (`test_spa_shell_is_revalidated_but_hashed_assets_are_cacheable`) hits `GET /`, but `main.py` only registers the SPA-serving route block `if STATIC_DIR/index.html exists` at import time — and `backend/app/static/` is gitignored, never committed, and never built by the `backend` job in `gate.yml`. On a clean CI checkout the route never registers, so `GET /` 404s for a reason unrelated to what the test checks; it only passed locally because a frontend build happened to already sit on disk from earlier work. Reproduced by removing `backend/app/static/` locally (404) and restoring it (pass). Fixed by adding a Node setup + `npm ci && npm run build` step to the `backend` gate job before `uv run pytest`, so CI matches what the Dockerfile's two-stage build already does in production. | done (2026-08-13) |
| G-60 | Improve | 🟡 | Tree UI | Found while reviewing the tree after the letterhead redesign: same-row peer ties for **non-adjacent** chips (sibling with someone in between, friend, colleague, custom) were drawn as a quadratic dip (`Q` curve) 20px below the chips — which grazes the family-bus channel (`BUS_OFFSET` 18px), so a peer arc and a bus rail ran 2px apart, and two peer arcs at different widths diverged: visible as a scratchy "double line" band between rows. Fixed by routing those ties through the same staggered channel allocator as the buses: each gets its own depth and runs as a right-angle elbow (down, across, up), matching the genealogy line language. Two regression tests: elbows contain no curves, and a peer tie sharing a channel with family buses sits ≥ `BUS_STEP` away from every rail. | done (2026-08-08) |
| G-59 | Bug | 🟡 | Frontend | **User-reported:** sensitive/password rows sit at a different indentation from the rest of the field list. Root cause: `FieldLabel` was a flex container, and a flex container's baseline is taken from its first item — the type *icon* (an SVG, whose baseline is its bottom margin edge). With a long label wrapped over two lines (emails-as-labels are the common sensitive case), the icon centered across both lines, so the row's `items-baseline` grid re-anchored the masked value + eye toggle to the *middle* of the label instead of the first line. Fixed by pinning the icon to the first line absolutely (`absolute left-0`, so it never drives the baseline) and giving the label a hanging indent (`pl-*` + `block`), so wrapped lines align under the text and every row — wrapped or not — aligns value-to-label on the first line. Verified with seeded email-style sensitive labels in light + dark screenshots. | done (2026-08-08) |
| G-58 | Bug | 🟡 | Frontend | Found while verifying the person-view redesign with seeded data: `FieldValue` renders a `textarea` value in a plain `<span>`, so line breaks are collapsed — "line one\nline two" displays as a single run-on line, on both the key-facts list and the field rows. Display-only, pre-existing (the old card had the same rendering). Fix with `whitespace-pre-line` on textarea values (and check the `break-words` interaction on long unbroken strings). | open |
| G-04 | Gap | 🟡 | Testing | **Mostly resolved 2026-08-01.** The DOM stack is now installed (`@testing-library/react`, `jest-dom`, `user-event`, `jsdom`) and wired into `vite.config.ts` with a jsdom environment and a setup file — which also had to polyfill `window.matchMedia`, since `useFlip.ts` calls it unconditionally on mount and jsdom doesn't implement it. Covered so far: tag chip add/remove, favorite toggle, index tag/favorite filters, star-doesn't-navigate, and a SEC-7 guard that a sensitive value stays masked until revealed. Severity dropped to 🟡; still uncovered: inline field edit, pin toggle, theme switch, drag-reorder, and the export/import Settings flows. | open |
| G-57 | Gap | 🟠 | Legal/compliance | Found while accepting G-05: `frontend/public/fonts/` ships 9 `.woff2` files and **no license**. Source Serif 4, Inter and IBM Plex Mono are all SIL OFL 1.1, which requires the license to travel with the fonts — the repo currently redistributes them bare. Needs `OFL.txt` plus a short README recording each file's provenance, version and subset. Left to the agent that produced the subsets, since only it knows the exact source. | open |
| G-18 | Improve | 🟠 | Security/UX | The 429 lockout response (G-07) doesn't send a `Retry-After` header with the remaining cooldown, and the UI doesn't distinguish "wrong password" from "temporarily locked". **Severity raised from 🟡 after this bit the owner for real (2026-08-01):** testing against their own data, they hit the 5-attempt limit, and every subsequent attempt — including with the correct password — was rejected with no indication that the rejection was temporary or when it would end. "Minor UX polish" was the wrong call. Send `Retry-After`, and have the login screen say plainly that the account is locked and for how long. Also reconsider the defaults: 5 attempts / 15 minutes is sensible for an internet-facing service but harsh for a single-user personal vault, where the only person it locks out is the owner — both are configurable (`DOSSIER_LOGIN_MAX_ATTEMPTS`, `DOSSIER_LOGIN_LOCKOUT_MINUTES`), so this is a defaults decision, not a code one. | open |
| G-23 | Gap | 🟠 | A11y | `Dialog` (ui.tsx) closes on backdrop click but has no Escape-to-close and no focus trap — keyboard users can tab out of an open modal. PLAN's "keyboard-operable dialogs" checkbox is optimistic. Add Escape handler + focus containment (or migrate to native `<dialog>`). | open |
| G-35 | Improve | 🟡 | Repo/DX | Found while building G-34: `PeopleRepository.list()` shadows the builtin `list` inside the class body, so **any** `list[...]` annotation on a method declared *after* it fails at import time with `TypeError: 'function' object is not subscriptable`. Worked around by declaring `list_with_details()` above `list()` (with a comment), but the trap stays for the next person. Fix properly with `from __future__ import annotations` in the module, or rename the method to `list_people()`. | open |
| G-36 | Improve | 🟡 | Export/backup | The JSON export (G-34) is structured data only — no document/photo bytes — so a user could mistake it for a full backup. Closed by the encrypted backup: `POST /api/backup` produces one AES-256-GCM archive holding the full export **and** the uploads directory, and restore puts the files back on disk and recreates the `Document` rows. The link that made this possible is `storage_path`/`photo_path`, which now exist on the export schema but are excluded from the plain API export payload (SEC-6) and populated only inside the encrypted archive — so the file mapping never leaves over the API. Verified live: backup → wipe → restore returned the document downloadable and the photo served. | done (2026-08-01) |
| G-53 | Improve | 🟠 | Frontend/mobile | **User-reported against the published v1:** the frontend is "meio ruim, meio pobre" — they want a cleaner, more current design, and stated the priority plainly: "gosto ainda mais de algo 100% funcional e compatível com mobile". Concrete defects behind that: the People index header uses `flex-wrap` with a fixed `w-56` search input and a `minmax(240px,1fr)` grid that breaks on a phone; the ID-card's `minmax(120px,240px)` dt/dd grid crushes values on narrow screens; icon buttons are 28–32 px against a 44 px touch-target minimum; `TopBar` is a fixed 56 px row with the wordmark and three buttons crowded together; and **field reorder is HTML5 native drag-and-drop, which does not work on touch at all** (the keyboard move buttons exist but aren't surfaced on mobile). Split in two once the mobile half landed. **Mobile/functional half: done (2026-08-01)** — verified independently in headless Chrome at 375×812 with touch emulation against the production build: **0 horizontal-scroll failures across 9 screens/states**, touch targets ≥44 px on 8 of 9. Delivered: a `can-hover` custom variant (touch fires a phantom hover on first tap, which made every hover-reveal control literally unreachable on a phone), an `IconButton` primitive (44 px mobile / 32 px desktop), `Dialog` as a bottom sheet plus a real Escape handler (half of G-23), WCAG 2.5.8 hit-area expansion (`before:-inset-2`) for dense chip controls with widened `gap-4` so neighbouring hit zones don't steal each other's taps, **ChevronUp/Down field reorder wired to the existing `onMoveUp`/`onMoveDown` props** (native HTML5 DnD never fires on touch, so reordering was impossible on a phone), a stacked dt/dd grid on the ID-card, a reflowed index header, and a genuine horizontal-overflow fix on the PersonPage action row (~358 px of content in a 343 px column with no wrap). Remaining: the export "include sensitive values" checkbox is 16×16 with a sibling label — deliberately left for a judgement call, since a larger target on a SEC-7 control also means easier accidental activation — and `FieldRow`'s reveal toggle is capped at 31×31 to avoid bleeding into adjacent rows. **Visual half: still open**, tracked as G-56. | done (2026-08-01) |
| G-56 | Improve | 🟠 | Frontend/design | The other half of the user's request — "um novo design melhorado e atualizado… formato mais clean" — is untouched: the mobile pass explicitly changed no colours, type scale or spacing tokens (its only new token was the `can-hover` variant). Open work: typographic scale and vertical rhythm are inconsistent across screens; surfaces, borders and shadows read generic; the ID-card (the centrepiece) still reads as a form rather than a record; empty/loading/error states are thin. G-05 (self-hosted Source Serif 4 / Inter / IBM Plex Mono woff2 — no CDN, SEC-9) belongs to this work and is likely the highest visual impact per unit of effort. Delivered by the frontend agent (T-03) and **accepted after independent verification**. Shipped: **G-05 closed** — 9 self-hosted woff2 subsets (192 KB) for Source Serif 4 / Inter / IBM Plex Mono, served from `/fonts` by the app itself, no CDN (SEC-9); the document type is what makes the ID-card finally read as a record rather than a form. The doc's type scale became real tokens (`--text-display/h1/h2/h3`) instead of `text-lg/xl/2xl/3xl` improvised per screen; elevation became theme-aware semantic variables (dark now uses dark shadows rather than the same light ones); the ID-card gained a mono `FILE № 0001 · ADDED …` meta line, per-section counts, and field-type badges; `Card` was extracted (Settings had six hand-rolled copies); the PersonPage 404 became a real `EmptyState` instead of a bare `<p>`; and a permanent black camera veil over the avatar on touch devices was replaced with a corner badge. **Verified here, not taken on report:** `npm run build` clean, 37/37 tests, 0 horizontal-scroll failures across 9 screens/states at 375 px, no external URL fetched at runtime (the three in the bundle are a comment, a React error string and an SVG xmlns), `/fonts/*.woff2` served with `font/woff2` + nosniff, no new dependencies, no hardcoded hex, and the G-48/49/54/55 query logic untouched. Known 4 px gap: the export "include sensitive values" row measures 40 px tall, not 44 (`py-2.5`); passes WCAG 2.5.8 AA (24 px) but sits under this project's own bar. Font licensing tracked separately as G-57. | done (2026-08-01) |

## Resolved

| ID | Type | Sev | Area | Finding & intended action | Status |
|---|---|---|---|---|---|
| G-54 | Bug | 🟠 | Tags UI | Found by review (T-02) of the G-49 optimistic update: an optimistic chip for a *new* tag carries a client-only placeholder id (`-Date.now()`), and the remove "X" was wired to it. Clicking X during the in-flight window sent `DELETE /api/people/{id}/tags/-1738…`, which the backend answers 404 → the rollback restored the snapshot that still held the placeholder and raised a bogus "Tag not found" alert; meanwhile the original assign resolved and re-inserted the tag, so **a tag the user had just removed reappeared** and needed a second click. Fixed by treating a placeholder chip as not-yet-removable: the X is `disabled` until the real id lands, and `removeTag` refuses a negative id even if something else calls it. Regression test drives assign → click X before the POST resolves → asserts no DELETE, no alert, and that the X becomes enabled once the real id arrives (verified failing without the fix). | done (2026-08-01) |
| G-55 | Bug | 🟠 | Tags UI | Found by review (T-02), pre-existing and the **same class as the user's original tag complaint**: `SettingsPage`'s `TagRow.invalidate()` refreshed `['tags']` and `['people']` but never `['person']` — a different key prefix — so renaming or deleting a tag in Settings left every already-visited ID-card showing the old chip name (or a chip for a deleted tag) with nothing scheduled to refresh it. Fixed by invalidating `['person']` too, with `refetchType: 'all'` so unmounted cards are reached, matching the `refreshLists` pattern from G-49. Regression test asserts the cached `['person', 7]` entry is invalidated after a rename (verified failing without the fix). | done (2026-08-01) |
| G-48 | Bug | 🔴 | Auth | **User-reported against the published v1:** "I type user and password, click sign in, the app clears the fields and stays on the login screen; only after a refresh does it let me in." Root cause: `Login`/`FirstRun` discarded the `AuthStatus` the login endpoint had just returned and re-derived auth state from a *second* round-trip (`invalidateQueries(['auth'])` → `GET /api/auth/status`). When that refetch fails or answers stale, React Query keeps the previous `{authenticated:false}`, the `Gate` in `router.tsx` redirects to `/login`, and `Login` remounts — so the typed credentials vanish. The mutation itself succeeded, so **no error is ever shown**, and the session cookie is already set, which is exactly why F5 "fixes" it. Reproduced in real headless Chrome over CDP by failing only the post-login `/api/auth/status`: identical symptom (empty fields, no error, still `/login`; reload → `/`). Fixed by seeding the cache with the response already in hand — `setQueryData(['auth'], status)` + `navigate(..., {replace:true})` — so signing in costs zero extra requests and cannot be undone by a flaky GET. Regression test asserts the user still lands on the index when that refetch throws. | done (2026-08-01) |
| G-49 | Bug | 🟠 | Tags UI | **User-reported:** "I add a tag and the page doesn't show it, only after I refresh; the per-tag person-count badge doesn't update either." `TagsSection` had no optimistic update: the chip appeared only after `POST` **plus** a full refetch of the person — two round-trips — while the input kept its text the whole time, so on any real latency it reads as "nothing happened". Measured under CDP network throttling: at 500 ms RTT the chip is still absent >1 s after Enter. Compounding it, `invalidateQueries` used the default `refetchType:'active'`, so the `['tags']` list backing the index badges was not refetched while the index was unmounted. Rewritten: `onMutate` applies the chip and clears the input immediately (`cancelQueries` + snapshot, rolled back on error), the new `person_count` is written straight from the assign response, invalidation uses `refetchType:'all'`, names are normalized exactly like the backend's `normalize_name`, a duplicate name is deduped instead of re-posted (an impatient second Enter no longer double-posts), and the datalist stops suggesting tags the person already wears. 5 new component tests. | done (2026-08-01) |
| G-50 | Improve | 🟠 | Auth/privacy | Found while fixing G-48: logout invalidated only `['auth']`, leaving every cached person, field, document and tag from the ended session in the React Query cache — re-rendered instantly if anyone signed back in, and readable from a devtools console meanwhile. Logout now calls `queryClient.clear()` before seeding the known unauthenticated status. Regression test asserts no `people`/`person` entries survive logout. | done (2026-08-01) |
| G-51 | Improve | 🟠 | Security | Found while fixing G-48: JSON API responses carried **no `Cache-Control` at all** — only file downloads did (G-11). A vault of personal records must not be written to a browser's disk cache or held by an intermediary proxy, and a cached `/api/auth/status` is precisely the stale-auth failure G-48 describes. The security middleware now sets `Cache-Control: no-store` on every `/api` response via `setdefault`, preserving the stricter `private, no-store` that downloads already declare. Scoped to `/api` so the content-hashed SPA bundle stays cacheable. | done (2026-08-01) |
| G-52 | Gap | 🟡 | Testing | Found while writing the G-48 regression test: Node 22+ ships an experimental global `localStorage` that **shadows jsdom's** and throws `getItem is not a function` unless the process was started with a valid `--localstorage-file`. Any test rendering `TopBar` (which calls `currentTheme()`) crashed into the router's error boundary instead of failing on its assertion — a trap that would have silently blocked testing every authenticated screen. `src/test/setup.ts` now installs an in-memory store when the global is unusable, alongside the existing `matchMedia` polyfill. | done (2026-08-01) |
| G-40 | Bug | 🔴 | Security | Found in review of the encrypted backup: `crypto.decrypt()` unpacked the Argon2id cost parameters from the archive header and passed them straight to the KDF **with no bounds check**. `memory_cost` is a `uint32`, so a crafted `.dossier` file could demand ~4 TiB of RAM; a forged header asking for 512 MiB (8× the default) was honored without complaint in a live test. Binding the header as AAD — which the implementation does correctly — does **not** cover this: it stops a *downgrade* of someone else's archive, but an attacker simply generates their own file with their own passphrase and absurd parameters, and the tag is valid for it. The tag doesn't even matter, because the expensive derivation runs **before** authentication can fail, so any garbage with a large number at bytes 13–16 forces the allocation. One small POST to `/api/restore` OOMs a single-container app; a bit-flip in that field on a corrupted download does it accidentally. Fixed by validating `time_cost`/`memory_cost`/`parallelism` against sane bounds immediately after unpacking and before deriving, rejecting (never clamping — a silent clamp derives the wrong key and reports it as a bad passphrase, which is a lie). Bounds: `time_cost` 1–10, `memory_cost` 8–262144 KiB, `parallelism` 1–16, applied on **encrypt** as well so the app can never mint an archive its own `decrypt` refuses to open. Re-verified after the fix: headers demanding 512 MiB, 2 GiB, and the uint32 maximum (~4 TiB) are all rejected in ~0.0000s, before any derivation, and legitimate archives still open. 8 new tests. | done (2026-08-01) |
| G-45 | Gap | 🟠 | Dependencies | Surfaced the first time the full gate ran under CI: `npm audit` reports 3 advisories. (1) **postcss ≤8.5.17** (high, GHSA-r28c-9q8g-f849, path traversal via sourcemap auto-loading) — dev-only, build-time, never shipped to a browser; fixable in place. (2) **react-router 6.0.0–7.17.0** (moderate ×2) — the app is on 6.30.4, so the fixed range starts at 7.18, making this a **v6→v7 major migration**, not a patch: deliberate work, not something to slip into an unrelated commit. Practical exposure here is low: `GHSA-337j-9hxr-rhxg` (constructor injection in SSR hydration) does not apply at all — this is a pure SPA with no SSR — and `GHSA-wrjc-x8rr-h8h6` (open redirect via backslash in `<Link>`/`useNavigate`) needs a navigation target derived from untrusted input, whereas every target here is an internal route built from a person id. Confirm that reading, then schedule the router upgrade. Separately: consider adding `npm audit --audit-level=high` to CI *after* these are resolved — adding it now would make the pipeline red on its first run, which is the fastest way to teach everyone to ignore it. | open |
| G-46 | Improve | 🟡 | Docs | `docs/Architecture.md` §12 lists `eslint` as part of the CI gate, but no ESLint config or lint script exists anywhere in `frontend/`. The CI workflow correctly mirrors the five commands that actually exist rather than inventing a step, so the doc is the thing that drifted. Either add ESLint (the frontend has real lint-able surface now) or drop it from the architecture doc — do not leave a documented gate that nothing enforces. | open |
| G-47 | Improve | 🟠 | Config/ops | Found while writing self-hosting instructions: `DOSSIER_SECRET_KEY` is declared in `config.py` with the value `"dev-insecure-change-me"` and `docker-compose.yml` tells operators to "override in production" — but **it is not used anywhere in the application**. A grep across `backend/app` finds no reader outside the settings class itself. Sessions are server-side random tokens and CSRF is double-submit, so nothing is signed with it. The harm is misdirection: someone hardening a deployment spends effort rotating a knob that does nothing, or worse assumes session integrity depends on it and reasons about the threat model incorrectly. Either wire it up (there is no current need) or delete it from `Settings`, the compose file, and the docs. Do not leave a security control that only pretends to exist. Resolved by deleting it: removed from `Settings`, `docker-compose.yml`, and the Architecture deployment example, with a comment in `config.py` recording *why* there is no signing key (so nobody re-adds one out of habit). | done (2026-08-01) |
| G-43 | Improve | 🟡 | Backup | Found answering a user question about restore safety. `BackupService.restore` extracts the archive's upload files to disk **before** running the DB import, so a file that decrypts fine but fails content validation (e.g. a tampered field value) rolls the database back cleanly while leaving the extracted uploads behind as orphans. Verified live: the restore returned 400, existing data was untouched, and `uploads/deadbeef.png` was still on disk. No data loss and no security exposure — the files have no rows pointing at them, so they're unreachable through the API — but nothing ever reclaims the space. Fix by extracting to a temp directory and moving files into `uploads/` only after the import commits, or by sweeping unreferenced upload files on startup alongside the existing expired-session purge (G-10). | open |
| G-44 | Gap | 🟠 | Backup/UX | Same review: "Restore from backup" is additive, so it **fills gaps but does not revert state** — a person who still exists is skipped by name, meaning an edited or emptied field is *not* rolled back to its backed-up value. That is the correct and safe behaviour (it can never destroy newer data), but "restore" reads to most people as "undo", and the Settings copy only says it "never deletes or overwrites". Say the limitation positively in the UI: this recovers what is missing, and to roll the whole vault back to an earlier state, use the `/data` directory backup documented in the README. | open |
| G-41 | Improve | 🟡 | Security/ops | Two smaller findings from the same review. (1) `BackupRequest.passphrase` carries no `max_length`: the bound was moved into the service so both the JSON and multipart routes return a uniform 400 instead of a 422, but that leaves the JSON body itself unbounded — the same shape as the import envelope, so not a new regression, but the app has no global request-body size limit and should get one. (2) `SystemSummary.database_bytes` measures only `app.db`, not the `-wal`/`-shm` companions, so the reported size understates reality while SQLite is in WAL mode; deliberate (deterministic for tests) but worth revisiting if the number ever drives a decision. | open |
| G-42 | Improve | 🟡 | Frontend | `apiBlob()` in `src/lib/api.ts` duplicates ~15 lines of `api()`'s preamble (CSRF header, `credentials: 'same-origin'`, `{detail}` → `ApiError`). It exists because the binary backup download can't go through `api()`, which always parses JSON, and touching `api()` would have put 20+ existing call sites at risk. Fold the shared preamble into one private helper both call, once there's a reason to open that file again. | open |
| G-39 | Bug | 🟠 | People | Found while adding favorites: making `PersonUpdate.full_name` optional (so a favorite toggle can be sent alone) opened a path where a client sending an explicit `{"full_name": null}` would attempt a NULL write into a NOT NULL column — a 500 instead of a validation error. `exclude_unset=True` doesn't help, because the key *is* set, just null. Fixed with a guard in `PeopleService.update()` raising `InvalidInputError` (400) when `full_name` is present-but-null, plus a regression test and a live check. Surfaced by the implementing agent, which flagged it rather than fixing it silently. | done (2026-08-01) |
| G-38 | Gap | — | Import | JSON import built (Phase 3, FR-30 / G3): `POST /api/import` consumes the G-34 envelope. **Additive by design — never destructive:** no deletes, no renames, no overwrites. People are matched by exact `full_name`; a match is skipped (counted + warned) and the file's links are reconnected to the existing record, which makes a re-import a no-op rather than a vault-duplicator. Imported people are built field-for-field from the file *without* the usual seeding, since the export already carries the system fields (seeding would double every built-in). `value_omitted` fields import blank and are counted, so a withheld secret can never null out a stored one (SEC-7). Every field value is validated against its type **before** the first write and the whole request shares one transaction, so a tampered or truncated file is refused whole rather than half-applied. Relationships are recreated through `RelationshipService` (so canonicalization, self-link and duplicate rules all still apply), resolving each side via the file's id map and falling back to an exact name match — a person-scoped export therefore links up to relatives already in the vault; unresolvable links are skipped with a warning naming the missing person. Roles survive the round-trip by choosing which side to treat as "related" and using the `child`/`godchild` alias when the younger side carries the role. Returns an `ImportReport` (created/skipped counts + warnings) that the Settings UI renders. 11 new pytest cases + live export→wipe→import→re-import round-trip (fields, sensitive value, "Mother" role label, and idempotency all confirmed). | done (2026-07-31) |
| G-37 | Bug | 🟠 | Fields | Found while adding the Date of birth field: **type validation was silently skipped on every field update.** `validate_value` compared with `is` (`field_type is FieldType.number`), but `PersonField.type` is a plain `String` column, so a field read back from the database carries a `str` — and `"number" is FieldType.number` is False. Identity matched nothing, so any PATCH that didn't also resend `type` (which is the normal case for the inline value editor) wrote unvalidated values: `"not-a-number"` into a number field, `"not-a-date"` into a date field. Only creation was safe, because there Pydantic hands over a real enum member. Fixed by comparing with `==` (StrEnum compares equal to its stored string) and widening the signature to `FieldType \| str`; grepped for the same `is`-against-StrEnum pattern elsewhere and found none. Regression test PATCHes value-only onto a stored number field and asserts 400 + unchanged stored value. | done (2026-07-31) |
| G-34 | Gap | — | Export | JSON export built (Phase 3, FR-30 / G3): `GET /api/people/{id}/export` and `GET /api/export` share one versioned envelope (`schema_version`, `generator`, `exported_at`, `scope`, `includes_sensitive_values`, `people`, `relationships`) so a future importer learns a single shape. **Sensitive values fail closed:** a `sensitive` field exports as `value: null` + `value_omitted: true` (the field's label/type/flags still travel so the record's shape round-trips), and plaintext values require an explicit `?include_sensitive=true` (SEC-7) — the Settings UI gates that behind an unchecked checkbox with a plaintext warning. Documents export as metadata only: no bytes, and `storage_path` is deliberately excluded so the random on-disk name never leaks (SEC-6). Relationships export in their stored canonical direction with both people's names denormalized. New `ExportService` + `schemas/export.py` + `routers/export.py`; `PeopleRepository.list_with_details()` added for the dataset scope; `download_filename()` extracted into `core/files.py` (shared with the vCard export) so a person's name can't inject a Content-Disposition header. 9 new pytest cases + live curl round-trip (attachment headers, nosniff, no-store, secret absent by default and present when opted in, 401 anonymous). Frontend delegated in parallel to a second agent via Maestri (PersonPage "Export JSON" link, Settings dataset export) and reviewed here; UI has no automated test — see G-04. | done (2026-07-31) |
| G-16 | Gap | 🟠 | Search/privacy | Field-value search (FR-27) must **exclude `sensitive` values** from any plaintext index (SEC-7). Resolved with FR-27: `GET /api/people?q=&fields=true` matches only non-sensitive field values via a correlated `EXISTS` (`type != sensitive`); `matched_fields` in the summary likewise skips sensitive fields, and the router recomputes matches over non-sensitive fields only. Regression tests assert a sensitive value makes nobody findable and never appears in the payload. | done (2026-07-07) |
| G-33 | Bug | 🟠 | Tree | User-reported with real data: the tree still looked "messed up" across views. Root cause was generation assignment, not layout — the per-center BFS gave every edge equal weight with first-path-wins, so a one-hop **godparent** shortcut beat the real blood path: a person who was both a grandfather and a godfather landed in *Parents* (gen −1 via godparent) instead of *Grandparents* (gen −2 via father→father), and in-laws landed level with the center. Because the winner depended on BFS order, the same family also drew differently depending on who was centered, plus long dotted godparent lines and curved ties crossed the chart. Fixed by making the chart **structurally stable** (user's "one tree, change the highlight" option): generation assignment is now two-wave — wave 1 walks only structural (parent/spouse/partner/sibling) edges to lay a consistent blood/marriage ladder, wave 2 places people reachable *only* via a soft edge (godparent shifts a generation, friend/colleague/custom stay level) and then re-expands their own structural subtree. Blood always wins, so the same family reads identically from any viewpoint; only the highlight + kinship captions recompute per center. A grandfather-who-is-also-godfather now shows "Grandfather" at gen −2. Frontend layout additionally drops soft ties (godparent/friend/…) whose endpoints are already blood-connected (union-find over parent edges) — those duplicate the family structure — and drops multi-row godparent links; the legend now reflects links actually drawn. 2 new backend tests (blood beats godparent shortcut; generations stable across two viewers) + 2 new frontend layout tests (drop redundant godparent, keep blood-less godparent). Verified live with Playwright on the reported 8-person family with 3 grandparents-as-godparents cross-links: Larissa/Fabio/Diego views now share one clean structure, correct generations, zero crossing/dotted clutter. | done (2026-07-06) |
| G-32 | Improve | 🟠 | Tree UI | User-reported with real data: the tree page looked "messed up" — generation rows used wrapping flexbox, so a full row wrapped chips onto a second line (a grandmother floated mid-chart), spouses could end up far apart (drawn as a chart-wide arc), and every parent edge drew its own elbow at the same midpoint so lines overlapped and crossed through chips. Rebuilt as a proper genealogy layout in a new pure module `frontend/src/lib/treeLayout.ts`: spouse/partner couples cluster into units (union-find), rows are ordered by 3 barycenter sweeps (down/up) to reduce crossings, x-positions pack per row then align outward from the center row so parents center over their children (greedy min-gap keeps rows overlap-free), rows never wrap (chart scrolls horizontally, centered when narrower than the viewport). Connectors are genealogy-style **family buses**: one drop from the couple tie (or each single parent), a horizontal rail staggered per row-channel so two families never share a y, then a drop to each child; godparent links get their own dotted rail; sibling ties are suppressed when the pair already shares a parent (the bus shows it); irregular cross-row edges keep a plain elbow fallback. TreePage now measures chip sizes in a hidden first pass and renders absolutely-positioned chips from the computed layout. Also **added the pending frontend test setup** (Vitest, `npm run test`): 7 unit tests for the layout engine (couple adjacency, parent centering, bus emission + sibling suppression, no-overlap, rail staggering). Verified live with Playwright against a replica of the reported family (2 grandparent couples, godmother link, 2 siblings): no wrapping, no crossings through chips, buses render as expected. | done (2026-07-06) |
| G-31 | Gap | — | Relationships | Relationship tree enriched (treemich-inspired, user request): (1) **new categories** — partner, friend, colleague, godparent/godchild (godchild canonicalizes to a godparent row exactly like child→parent; godparent shifts a generation in the tree, social types are peers with distinct dash patterns + legend entries); (2) **gendered roles** — optional `related_role` on create (mother/father/son/daughter/brother/sister/husband/wife/god-*) stored per side (`role_a`/`role_b`, new migration), must refine its structural type or 400; the counterpart's role becomes their card label ("Mother" instead of "Parent"), unknown sides stay generic; (3) **derived kinship captions** — new pure `services/kinship.py` names each tree node's relationship to the center from its BFS path ("Father of X", "Grandmother of X", "Uncle of X", "Cousin of X", sibling-via-shared-parent, in-law/step terms via one spouse hop at either path end), gendered by recorded roles with neutral fallbacks, None for unnameable paths; rendered as a small caption inside each chip. vCard RELATED TYPE mapping extended from the 4 standard types to all role labels + friend/colleague/sweetheart/kin (RFC 6350 §6.6.6). Add-relationship dialog now uses grouped options (Family/Partners/Godfamily/Social/Other) where picking "Mother" implies type+role. 16 new pytest cases (incl. kinship unit tests) + live curl smoke: three-generation family with uncle & godmother resolves Father/Mother/Grandmother/Uncle/Godmother captions and correct generations. | done (2026-07-06) |
| G-29 | Bug | 🟠 | Layout | User-reported with real data: (1) index-grid avatars rendered at slightly different sizes — `Avatar` had no `shrink-0`, so flex rows squeezed the circle in cards with long text while short cards kept 56px; (2) ID-card header values sat visibly lower than their labels — the dt/dd grid lacked `items-baseline` (label-caps is 16px line-height vs 22px value text); (3) long unbreakable field labels (emails/codes used as labels) collided with the value column — grid cells needed `min-w-0` + `break-words`; header label column capped at `minmax(120px,240px)` so a long label wraps instead of shoving values right. Verified in dark mode with the reported data patterns: avatars all measure exactly 56×56, zero label/value overlaps. | done (2026-07-06) |
| G-30 | Bug | 🔴 | Security | Found while fixing G-29: the index-grid pinned preview rendered field values **including `sensitive`-type values raw** — pinning a sensitive field put its secret unmasked on the People grid (SEC-7 violation; the grid has no reveal control, unlike the card). Fixed: `_summary` excludes sensitive fields from `pinned_fields` entirely. Regression test added (value absent from the API payload, not just hidden). | done (2026-07-06) |
| G-28 | Gap | — | Relationships | People relationship tree view built (Phase 2b): `GET /api/people/{id}/tree` BFS-walks the connected component (±3 generations, 100-node cap; parent edges shift a generation, spouse/sibling/custom are peers; first assignment wins on conflicting paths). New `/people/{id}/tree` page renders generation rows with ledger-margin labels (Grandparents/Parents/This generation/…, Great- prefixes beyond), hand-rolled SVG connectors measured from node positions (parent elbows, spouse ties, dashed sibling, dotted custom with a label pill placed below chips so it never covers a name), amber seal ring on the center chip, all nodes navigable. Spouse-linked people are seated adjacent within a row via a small union-find pass. No graph library added — stays within existing deps per CLAUDE.md. Entry: "View tree" in the Relationships section. 5 new pytest cases + live Playwright pass with a 3-generation family, per-viewer reframing (kid's tree shows Grandparents at −2), and empty state. | done (2026-07-06) |
| G-27 | Gap | — | Export | vCard export for a person built (Phase 3, new idea, FR-30 sibling): `GET /api/people/{id}/vcard` produces vCard 4.0 text via a new `VCardService`. Field mapping: label containing "email"/"phone"/"mobile" → EMAIL/TEL, exact "Address" → ADR, everything else non-empty → NOTE line; `sensitive`-type field values are always excluded, matching SEC-7. Relationships appear as RELATED lines (TYPE param for the four standard types). Text values escaped per RFC 6350 §3.4 (backslash/comma/semicolon/newline) so freeform field values with punctuation don't produce a malformed file. Filename is ASCII-sanitized to avoid Content-Disposition header injection. Deliberately skips PHOTO embedding and line-folding for >75-octet lines — both are optional per spec and most modern parsers (Apple/Google Contacts) tolerate omitting them; noted as possible follow-ups, not required. 7 new pytest cases + live Playwright download verification. | done (2026-07-05) |
| G-26 | Gap | — | Relationships | Phase 2 Epic E built end-to-end: repository (`list_for_person`/`exists`/CRUD), service (canonicalizes `child`→`parent` swap so the pair/type stays unique regardless of which side links first; resolves the correct inverse label per viewer — spouse/sibling symmetric, parent↔child inverted, custom mirrors `custom_label`), schemas, router (`POST`/`DELETE /api/relationships`), inclusion in `GET /api/people/{id}`. Rejects self-links (400) and duplicates in either stored order (409); custom type requires a label (400). Frontend: `RelationshipSection` with grouped chips (avatar + name, Design System §5.5), add-relationship dialog (person search + type + custom label), remove-on-hover. `SectionHeading` extracted from `PersonPage` into shared `ui.tsx` since three sections now use it. 8 new pytest cases + full Playwright pass (parent/child inverse labels on both cards, chip navigation, add/remove). | done (2026-07-05) |
| G-24 | Improve | 🟠 | Fields | Seeded default fields (Document number, Address, Nationality) were fully mutable — deletable, renamable, reorderable — despite being the record's identity backbone (FR-17). Added `is_system` flag: model + migration (`server_default='0'` + label-based backfill of existing rows), seeded true, exposed in `FieldOut`. Service rejects delete/rename/type-change with 400 (value + pin stay editable); UI hides drag handle + delete on system rows, locks the label in edit mode, and marks each with a quiet 14px icon (IdCard/MapPin/Globe). Drag groups extended to system/pinned/unpinned. Verified via new pytest case + Playwright incl. raw-API delete → 400. | done (2026-07-05) |
| G-25 | Bug | 🟡 | Fields | Follow-on to G-24: system fields could still be *unpinned* (pin toggle stayed enabled), letting Document number/Address drop out of the card header they're meant to permanently anchor. Fixed: service rejects `is_pinned` changes on system fields with 400; UI hides the pin button on system rows (only edit remains); data migration re-pins any system field unpinned before this rule existed. | done (2026-07-05) |
| G-20 | Bug | 🔴 | Fields UI | Field drag-and-drop never started in **Firefox**: the handle's `dragstart` never called `dataTransfer.setData()`, which Firefox requires to initiate a drag (Chrome doesn't). Fixed alongside G-22; also set `effectAllowed='move'` and `setDragImage` to the whole row so the drag ghost is the full row, not the tiny handle button. | done (2026-07-05) |
| G-21 | Bug | 🟡 | Fields UI | If the reorder API call failed, the optimistic local order stayed on screen (mutation only invalidated on success) — UI and server silently diverged until the next refetch. Fixed: `onError: invalidate` reverts the list. | done (2026-07-05) |
| G-22 | Improve | 🟠 | Fields UI | Reorder/hover interactions felt abrupt: rows teleported on reorder, no row hover feedback, static drop line at the wrong edge for downward drags. Added: FLIP slide animation (180ms ease-out via Web Animations API, respects `prefers-reduced-motion`) on drag *and* keyboard reorder; `--surface-hover` row hover tint per Design System §3; accent insertion bar that scales in and sits on the true insertion edge (bottom when dragging down, top when up); `aria-live` announcement of the new position for keyboard moves. All within Design System §1 "calm motion" (120–200ms ease-out). | done (2026-07-05) |
| G-19 | Bug | 🟠 | Fields UI | User-reported: "drag down doesn't work, only drag up." Root cause: dragging *within* the pinned or unpinned group works either direction; the failure was dragging a pinned field (always at the top, incl. the 3 seeded fields every person starts with) *down* across the pinned/unpinned boundary — the intentional group-confinement guard from G-01 silently no-opped with zero feedback, so it looked broken. "Drag up" rarely hits this boundary in casual testing, hence the asymmetric symptom. Fixed: cross-group drag now shows a "not-allowed" cursor and skips the drop-target highlight instead of silently doing nothing; confirmed with the user that groups should stay separate (not merge into one freely-orderable list). | done (2026-07-05) |
| G-02 | Gap | 🟡 | Documents UI | Document rename not surfaced (PATCH endpoint existed). Built inline rename on `DocumentRow` mirroring `FieldRow`'s edit pattern (pencil → input → save/cancel). Also fixed a docstring inaccuracy while here: `DocumentService.upload`'s default title is the filename's *stem* (no extension), not the full filename as documented. Verified live via Playwright: rename persists across reload, cancel reverts, empty title blocked by required validation. | done (2026-07-04) |
| G-07 | Gap | 🟠 | Security | No login rate-limiting/lockout on repeated failures. Added: `User.failed_login_attempts`/`locked_until` columns; after `DOSSIER_LOGIN_MAX_ATTEMPTS` (default 5) consecutive failures the account locks for `DOSSIER_LOGIN_LOCKOUT_MINUTES` (default 15), rejecting even correct credentials with 429 until it expires. Verified via a new pytest case and live curl against the dev server. | done (2026-07-04) |
| G-01 | Gap | 🟠 | Fields UI | Drag-and-drop reorder not built (reorder API + test existed). Built: native HTML5 DnD via a grip handle (`⇅`, matches Design System §5.3) on each `FieldRow`, plus ArrowUp/ArrowDown keyboard support on the same handle for a11y (NFR-9 — native DnD alone isn't keyboard-operable). Reordering is confined to each field's pinned/unpinned group, since the list always displays pinned fields first regardless of stored `position` — a cross-group move would silently revert once the list refetched. Verified live via Playwright: drag persists across reload, keyboard move works, cross-group drop is correctly rejected. | done (2026-07-04) |
| G-17 | Bug | 🔴 | Docker build | No `.dockerignore` existed: `COPY backend/ ./` and `COPY frontend/ ./` copied the host's own `backend/.venv` and `frontend/node_modules` over the image's freshly built ones (no correcting copy after, unlike `app/static`). Container crash-looped: `.venv` pointed at the host's pyenv interpreter, so `uv run` discarded it, recreated an empty one, and `uvicorn` was never installed into it. Fixed: added root `.dockerignore` excluding `.venv/`, `node_modules/`, caches, `data/`. | done (2026-07-04) |
| G-06 | Gap | 🟠 | Deploy | Docker image never built/run end-to-end. Built + ran via `docker compose up`; verified health check, SPA serving, first-run admin setup via API, data written to `./data`, session + data surviving both `restart` and full `down`/`up` recreation, and backup/restore round-trip (stop → tar → wipe `data/` → untar → start → still authenticated). Also cross-built for `linux/amd64` (native arch is arm64) confirming NFR-1 multi-arch. | done (2026-07-04) |
| G-08 | Gap | 🟠 | Repo | Project was not under version control. Confirmed: `git init` done, `origin` remote set to github.com/DiegoGarzaro/dossier, initial commit landed, `uv.lock` + `package-lock.json` tracked. | done (2026-07-04) |
| G-03 | Gap | 🟠 | Security/ops | `DOSSIER_TRUST_PROXY` declared but never consumed. Wired: `secure = settings.trust_proxy or request.url.scheme == "https"` in `auth.py` and `middleware.py`. | done (2026-07-04) |
| G-09 | Bug | 🟡 | People UI | Index-grid thumbnail URL had no cache-bust → stale photo after change. Fixed: added `updated_at` to `PersonSummary` schema + `?v=<updated_at>` param on grid photo URLs. | done (2026-07-04) |
| G-10 | Gap | 🟠 | Auth/DB | Expired `sessions` rows never purged → unbounded table growth. Fixed: `SessionRepository.purge_expired()` called in app `lifespan` on startup. | done (2026-07-04) |
| G-11 | Improve | 🟡 | Security | Photo/document `FileResponse`s had no `Cache-Control`. Fixed: added `Cache-Control: private, no-store` to both photo and document download responses. | done (2026-07-04) |
| G-12 | Improve | 🟡 | Validation | Field `value` had no length cap. Fixed: `max_length=10_000` added to `FieldCreate.value` and `FieldUpdate.value` in `schemas/field.py`. | done (2026-07-04) |
| G-13 | Improve | 🟠 | Frontend | No React error boundary. Fixed: `ErrorBoundary` class component added to `ui.tsx`, wraps the entire app in `main.tsx`. | done (2026-07-04) |
| G-14 | Improve | 🟡 | Ops | `/api/system/health` returned ok without checking DB. Fixed: endpoint now runs `SELECT 1` via `DbSession` dependency. | done (2026-07-04) |
| G-15 | Improve | 🟡 | Validation | `float()` accepts `inf`/`nan` in number-field validation. Fixed: `math.isfinite()` check added after parse in `field_service.py`. | done (2026-07-04) |
